'use strict';

let colors = ['red', 'green', 'blue'];
colors.forEach(function (color) {
    console.log(color);
}); // red, green, blue

// colors.forEach((color) => console.log(color)); --- IGNORE ---
